local config = {
    hitboxes = false,
    borders = false,
    shaders = true,
    lighting = true,
    physics = true,
    entities = true,
    blocks = true,
    vsync = true,
    palette_index = 1
}

return config